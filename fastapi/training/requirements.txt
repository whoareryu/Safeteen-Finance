# Qwen3-4B-Instruct QLoRA 파인튜닝 전용 환경.
# fastapi/requirements.txt(서빙용)와 완전히 분리 — Docker 빌드 컨텍스트(./fastapi) 밖이라
# 프로덕션 이미지에는 절대 포함되지 않는다. 원격 PC(RTX 3050, CUDA) 전용 venv에서만 설치.
#
# torch는 여기 포함하지 않는다 — README 안내대로 드라이버(CUDA 12.6)에 맞는 cu126
# 인덱스로 먼저 설치한다.
#
# 아래 버전 조합은 이전에 시험했던 EXAONE 모델(trust_remote_code 커스텀 코드가
# transformers v5 전용 심볼을 요구) 기준으로 고정된 것이다. peft를 0.13.x로
# 낮춘 이유도 당시 EXAONE의 get_input_embeddings 미구현 문제를 경고로만 넘기기
# 위함이었다 — Qwen3 등 네이티브 아키텍처 모델은 이 문제가 없어 더 낮은 조합도
# 되지만, 이미 검증된 조합이라 그대로 둔다.
transformers>=5.0,<6.0
peft==0.13.2
bitsandbytes>=0.44.0
accelerate>=1.0.0
trl>=1.0.0,<2.0.0
datasets>=3.0.0
huggingface_hub>=0.25.0
sentencepiece
