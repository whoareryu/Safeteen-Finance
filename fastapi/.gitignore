# --- 비밀·자격 증명 (GitHub에 올리면 안 되는 정보) ---
.env
.env.*
!.env.example
!.env.sample
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*_rsa
*_ed25519
*_ecdsa
*.pem.pub
secrets/
.secrets/
credentials.json
*credentials*.json
service-account*.json
.aws/credentials
.azure/
gcloud/
*.db-journal

# --- API 키·토큰이 들어가기 쉬운 로컬 설정 ---
*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/
.eggs/
*.egg-info/
*.egg
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.cover
.tox/
.nox/

# --- Node / 프론트엔드 ---
node_modules/
.npm
.pnpm-store/
.next/
out/
.nuxt/
.vite/
*.tsbuildinfo

# --- IDE·에디터 (개인/기기별 경로가 섞일 수 있음) ---
.idea/
*.swp
*.swo
*~

# --- OS ---
.DS_Store
Thumbs.db
Desktop.ini

# --- 로그·덤프·임시 ---
*.log
logs/
*.tmp
*.temp
*.bak
*.dump

# --- Jupyter ---
.ipynb_checkpoints/

# --- 기타 로컬 전용 ---
.cache/
*.sqlite3
*.db



# 대용량 원본 데이터 (CSV 등) — git 미포함
apps/restaurant/data/raw/

# --- YOLO 등 실행 시 자동 다운로드되는 모델 가중치·샘플 ---
*.pt
# plant_yolo.pt는 자동 다운로드가 아니라 직접 학습한 진단 모델(5MB 남짓)이라
# 배포 이미지에 포함되어야 한다 — 제외 대상에서 예외 처리.
!apps/plant/resources/plant_yolo.pt
*.cache
bus.jpg
runs/

# --- CatBoost 학습 시 자동 생성되는 로컬 산출물 ---
catboost_info/
titanic_correlation.png

# --- HuggingFace 로컬 모델 캐시 (대용량) ---
models/
/Qwen--Qwen2.5-1.5B-Instruct/

# 얼굴 인식 학습용 원본 이미지 (인물 사진 — git 미포함)
apps/ontology/resources/yolo_train/train/
apps/ontology/resources/yolo_train/val/

# 식물 진단 학습용 원본 이미지 (PlantDoc 데이터셋 — git 미포함)
apps/plant/resources/yolo_train/train/
apps/plant/resources/yolo_train/val/

# 크롤러/스크래퍼 실행 산출물 (JSONL — git 미포함)
apps/ontology/resources/crawled/*.jsonl
apps/ontology/resources/scraped/*.jsonl
apps/plant/resources/scraped/*.jsonl

# 식물 진단 업로드 사진 (S3 발급 전 임시 로컬 저장 — git 미포함)
apps/plant/resources/diagnosis_uploads/
