# FastAPI 백엔드 (next.config.mjs rewrites) — Vercel 환경 변수에도 동일 키로 등록
# 로컬
BACKEND_URL=http://127.0.0.1:8000
# 프로덕션 예: BACKEND_URL=https://your-api.railway.app 또는 공개 백엔드 URL

# 날씨·Kakao·LOCALDATA 키는 backend/.env 에만 넣습니다 (frontend 불필요).
