# FastAPI 백엔드 (next.config rewrites → POST /chat)
BACKEND_URL=http://127.0.0.1:8000

# OpenWeatherMap — https://home.openweathermap.org/api_keys
# 실제 키는 frontend/.env.local 에 넣고 Git에 올리지 마세요.
OPENWEATHER_API_KEY=
# 위치 권한 거부 시에만 사용 (기본 Seoul). 허용 시 브라우저 GPS 좌표로 조회합니다.
OPENWEATHER_CITY=Seoul
